# Rust Cargo project files
# -------------------------
# Standard build output directory
target/

# Rust-specific cache and metadata files
# For Cargo workspace dependencies (usually in ~/.cargo/registry)
# or when building a single package outside a workspace
.cargo/
.rustc_info.json

# Dependencies lock file (generally NOT ignored for application projects)
# If this is a library, you MIGHT want to ignore Cargo.lock
# but for applications, it's typically committed to ensure reproducible builds.
# # Cargo.lock

# Build artifacts for specific tools (e.g., rust-analyzer)
# If you are using rust-analyzer, these files are generated in the project root
# or a specific workspace root.
.rust_analyzer/
.clippy/

# For generated documentation
doc/

# Editor/IDE specific files
# -------------------------
# VS Code
.vscode/
# Add common settings.json, launch.json, tasks.json if they contain local paths
# or user-specific configuration not meant to be shared.
# e.g., .vscode/*.json if you want to be more specific.

# IntelliJ IDEA / RustRover
.idea/
*.iml
.DS_Store # macOS specific
Thumbs.db # Windows specific
ehthumbs.db
.directory

# Vim/Neovim swap files, backup files, etc.
*~
*.swp
*.swo
*.bak
*.tmp

# Emacs backup files
#*#
.#*

# Build tools and other generated files
# -----------------------------------
# Output from specific rust tools or test runs
*.profdata
*.d
*.gcno
*.gcda
*.gcov
*.ll
*.bc

# Environment files (e.g., for local secrets or variables)
.env
.env.*
!.env.example # Keep .env.example if you provide one for reference

# Operating System specific files
# -------------------------------
# macOS
.DS_Store
.localized
.Spotlight-V100
.Trashes
__MACOSX

# Windows
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/

# Linux
# No common specific files to ignore beyond general ones.

# Miscellaneous
# -------------
# Executables generated by `cargo install` locally
# Not usually in project root, but good to include if you ever install things here.
*.exe
*.dll
*.so
*.dylib
*.pdb

# Crash dumps
*.dmp

# Editor/OS specific temp files
*.log
*.sqlite
*.sql